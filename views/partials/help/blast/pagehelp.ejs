
Select a database first then paste a sequence (or multiple sequences in FASTA format) into the
sequence box. You can change the default BLAST options. 
The BLAST button should now be active.

The results page will have a link that you can copy to be able to re-visit the results page for up to 30 days.
